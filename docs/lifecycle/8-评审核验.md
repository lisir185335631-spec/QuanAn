# 阶段8 评审与安全核验报告 · QuanAn

> 2026-06-20 · 阶段7 验收带债出门后的评审门禁 · 对照 `KB/2-standards/checklists/08-评审与安全清单.md`
> 范围:① 核验阶段6/7 充分性 ② 回补阶段7 欠的交付能力包(eval-suite / redteam / G71)③ 按《Agent项目架构实质性标准》L3 接地真做、不做表面壳。
> 证据规则:每条以 `文件:行号` / 命令输出 / 双向自测真数据留痕,非主观判断。
> 真值口径(用户 2026-06-20 拍板):**机制做实·真值待真跑** —— 不依赖外部 LLM 的实质全做实并真跑出真数据;kappa≥0.6 真值与 promptfoo 端到端 LLM-ASR<5% 需 API Key,标"机制就绪·待真跑回填",**绝不编造假数值**。

---

## 〇、方法与定位

阶段8 = 上线前最后一道评审门禁。本轮方法:
1. **回源盘点**:读《Agent项目架构实质性标准》+ 08 清单 + 双维映射阶段8 簇 + 模板注册表(交付能力包挂阶段/触发画像)+ 12 维画像。
2. **现状深盘**(team 模式·2 Sonnet 并行,带 file:line)。
3. **Opus 逐条回源亲核**(拦截 agent confident-wrong)。
4. **实装工件**(team 模式·Sonnet 执行)→ **Opus 复跑坐实**(不轻信报告)。

> 母题贯穿:**流程合规 ≠ 产物实质**。本报告每个"通过"都附可证伪证据;每个"待真跑/软肋"都如实标注,不灌水。

---

## 一、Opus 亲核纠偏(评审门禁的核心价值)

Sonnet 深盘报了 2 个"P0/P1 真漏洞",**经 Opus 回源亲核确认是误报**——被架构文档旧描述(写于阶段3)误导:

| Sonnet 报 | Opus 亲核真相 | 证据 |
|---|---|---|
| R-14 PrivateDomainAgent 绕过 piiMask、自建 systemPrompt(引旧行号 165-188/220-228) | **阶段6 已修干净** | `PrivateDomainAgent.ts:131` extends BaseSpecialist · `:164` 用 `ctx.systemPrompt`(走中心 ContextAssembler,与 R-11 audit✅ 一致)· `:187-190` 4 个自由文本字段全 `piiMask`(R-14/US-002 AC-5);文件仅 214 行,agent 引的行号是文档旧值 |
| R-21 trending_favorites 无 RLS policy | **阶段6 已修干净** | `manual_rls.sql:32` ENABLE RLS · `:102` `trending_favorites_account_isolation` policy · `:173` admin bypass |

→ **真问题 = 架构文档滞后(文档债)**:`docs/architecture/02:45,55`、`06:31-46` 仍写 R-14/R-21"真缺陷→阶段6修"+旧行号,代码改了文档没回填,违《Agent项目架构实质性标准》三·接地铁律的 from-0 回填卡口。**本轮已回填**(标"阶段3揪出→阶段6已修+真实行号",保留历史可审计)。

> 评审价值实证:**不亲核就会去"修"已修好的代码(甚至改坏)、并误判阶段6/7 假勾**。这正是阶段8 评审门禁该揪的:文档↔实现脱节。

---

## 二、交付能力工件落地(画像命中 · 回补阶段7 欠债 · 在现有基建上扩)

### 2.1 eval-suite(P12=混合RAG·需效果评估)

**现状=在丰富的现有基建上扩,非从0**:`tests/judge/judge-runner.ts` 真调 llmGateway(非 mock,19 judge 文件 `skipIf(!ANTHROPIC_API_KEY)`)· `tests/fixtures/judge-goldens/` 100 条真实三元组 · `apps/api/src/lib/evaluation/inter-rater.ts` 真 Cohen's kappa(算法完整+单测)· admin evaluation 三页 UI 完整。

**本会话做实**(命令坐实):
- ⛔ **CI 回归门禁**(原完全缺·最大块):新建 `.github/workflows/eval-regression.yml`(PR+main push 触发;Golden schema 验证 + `pnpm test:judge`,无 KEY→skipIf 全 skip 退出0 不卡 PR,有 secret KEY→真跑回归)。**复跑坐实**:`pnpm test:judge` → `40 skipped, exit 0`。
- **阈值对齐 gate 0.6**:`InterRaterPage.tsx:516,536,541` + `EvaluationDetailPage.tsx:357-358` κ 门槛 0.4→0.6(留注释"PRD-28 历史 0.4→对齐 KB gate 0.6");`inter-rater.ts:96` 的 0.4 是 Cohen's kappa 标准解读区间(moderate)非门禁,正确未改。
- **Golden 配额**:`custom-70.json` 补 `custom-068`(EvolutionAgent)、`custom-072`(DiagnosisAgent)各 1 条真实三元组 → Diagnosis/Evolution 各达配额 2 条;**附带修复存量 ID 重复 bug**(custom-062 重复→改 custom-071);总数 70→72,唯一 ID 72,`dataset-validation` 断言同步。

**待真跑**:kappa≥0.6 真值需 `pnpm eval:run` + ANTHROPIC_API_KEY + admin 人工打分 30 条(机制全就绪,`.agents/verification/prd-28-evaluation.md:194,211` 标注)。

### 2.2 redteam-promptfoo(P3=公网云 + 含 RAG)

**从0 建**(原无任何红队脚本/ASR):
- **实例化** `scripts/redteam/`(security-redteam.yaml 24 用例 + run-redteam.sh + redteam_cases.md 七类攻击 QuanAn 具体化 + README)。
- **防御层拦截率真数据**(本会话真跑·不依赖外部 LLM):`apps/api/src/lib/security/__tests__/injection-defense.redteam.test.ts` 35 个真实中文攻击 case 七类,对 `detectInjection` 纯函数跑出拦截率。
- **红队闭环**(Opus 判定):红队真跑揪出 injection-filter 3 个真实漏拦盲区 → **盲区1(jailbroken 不规则过去分词)、盲区2(PL-03"告诉我你的系统提示")闭环修复**;**盲区3(II-02"AI系统:"中文前缀)接受为已知软肋**(宽化中文"系统:"前缀会误伤营销文案"智能系统:xxx",与 injection-filter "narrowing 避免误报"哲学冲突;过滤的是 RAG 中间层,单盲区可接受)。
- 综合拦截率:**91.4%(35 case)→ 闭环修复后 97.1%**(34/35,仅余盲区3 软肋)。

**待真跑**:promptfoo 端到端 LLM-ASR<5% 需 ANTHROPIC_API_KEY(`cd scripts/redteam && ./run-redteam.sh`),机制就绪·待真跑。

### 2.3 G71 平台发布合规预检(P1=AI IP 营销内容)

**在 output-guardrail 上扩,非另起炉灶**(banned-word.service 默认 mock+远程 webhook 不适合同步扫描,未用):
- `output-guardrail.ts:42-118` 增 `AD_LAW_PATTERNS`(《广告法》第九条 9 类绝对化用语:国家级/世界级、排他性第一/唯一/独家、最高级形容词、顶级极致、史无前例类成语、市场领导、价格极限、无敌无可比拟、正宗纯正)+ `detectAdLawViolation()`(`:125-136`),收窄避免误伤(第一排除"步/次/个/章",最排除"的问题/方式")。
- 接进 `checkOutput` Step3(`:210-227`)→ `scanObjectOutput` 递归(`:236-258`)→ `BaseSpecialist._applyOutputGuardrail` 真接 specialist 文案输出端。
- **双向自测**(复跑坐实):`pnpm vitest output-guardrail.test.ts` → **45 passed**(正向极限词检出 + 负向正常文案零误检)。

**轻债**:平台特定规则(抖音/小红书导流限制)本次不做,记后续。

### 2.4 quality-verifiers — N/A

画像不命中:P2=创作容错(非数字零容错)· specialist 单步(95/5 无多步规划)· 流式 SSE 仅 TopicAgent 且 10.6b 已是 QuanAn 实测沉淀。判 N/A 有画像依据,不参与短板计算。

---

## 三、08 清单逐项核验

### A. PRD vs 实现最终核验
- ⛔ US 全有实现、AC `passes:true`(承接阶段7 验收报告 A 节·prd.json US-001~011)。✅

### B. 代码审查
- ⛔ 无硬编码 Secret:`audit-redlines.sh` 全绿(本会话 4 次 commit pre-commit hook 实跑 0 命中)。✅
- 无 console.log/debugger(audit coding-standards §6.9)。✅

### C. 安全审查 — OWASP LLM Top 10
| 项 | 判定 | 证据 |
|---|---|---|
| ⛔ LLM01 直接注入 | ✅ 防御+红队验证 | `injection-filter.ts` 4 类 pattern;红队拦截率 97.1% |
| ⛔ LLM01 间接注入 | ✅ | RAG chunks+stepData 进 ContextAssembler 前 `detectInjection`;红队间接注入 case 真跑 |
| ⛔ LLM02 PII 泄露 | ✅ | 7 家字段级 piiMask(含 PrivateDomain `:187-190` 已修)+ 输出端 maskOutputString |
| ⛔ LLM05 输出处理 | ✅ | 所有 specialist 输出 Zod `safeParse`,不 eval/exec |
| ⛔ LLM06 过度授权 | ✅(单次架构) | 工具枚举限定(`tools:[]`/`['llm.complete']`),无外部副作用工具;非 ReAct loop |
| ⛔ LLM07 系统提示泄露 | ✅(prod)+软肋(dev) | `evolution.ts` debugAssembleSystemPrompt 在 prod `throw FORBIDDEN`;dev 返回(仅 protectedProcedure)=软肋,记 |
| ⛔ LLM08 向量库安全 | ✅ | knowledge_chunk 全局共享知识库(设计·非用户私有→N/A 跨租户);trending_favorites RLS 已修 |
| ⛔ LLM10 资源耗尽 | ✅ | `cost-budget-guard.ts:16` MAX_RUN_COST_USD=0.5 超抛 BudgetExceededError;单次调用架构无 Agent 循环;红队资源耗尽 case 真跑(addCost→checkBudget→抛错) |

### D. 访问控制与鉴权
- ⛔ API 端点鉴权(protectedProcedure 401/403)✅ · ⛔ 租户隔离(查询带 accountId)✅ · ⛔ RLS(`manual_rls.sql` 16 表 kernel 级)✅ · ⛔ 高权限操作审计日志(admin_audit_log)✅

### E. Red Teaming(关键功能必做)
| ⛔ | 判定 | 证据 |
|---|---|---|
| 直接注入测试 | ✅ 真跑 | 直接指令覆盖 8/8 + 分隔符逃逸 4/4 = 100%;闭环后角色劫持/提示泄漏补齐 |
| 间接注入测试 | ✅ 真跑 | RAG chunk 恶意指令 case(4/5,余 II-02 软肋);ContextAssembler 集成测试覆盖 |
| 越权测试 | ✅ 真跑(RLS 语义) | 注入+越权组合被拦;纯业务越权由 RLS 16 表兜底(返 0 行=隔离生效,**RLS 架构下语义=0 行而非传统 403**);`account-isolation.spec.ts` E2E |
| 资源耗尽测试 | ✅ 真跑 | `addCost(0.45)+checkBudget(0.1)`→BudgetExceededError(0.55>0.5);cost-budget-guard.test 覆盖 |

> 端到端 LLM-ASR<5%(promptfoo 经真 LLM)待真跑(需 KEY),机制就绪。

### F. 合规检查
- ⛔ 用户数据删除接口(GDPR):**N/A**(P11=无特殊·非 GDPR/数据出境,02 阶段 cross_2 已判 na)
- ⛔ 不涉及法律/医疗最终决策:✅(P1=营销内容创作,无法律医疗决策)
- ⛔ 生产日志不含完整 prompt:✅(cost-log 记 tokens/cost 非 prompt 原文;dev debug 端点 prod 屏蔽)
- 数据留存策略:admin_audit_log 留痕(运营期细化)

### G. 交付能力落地(画像命中必检)
- ⛔ **G70 redline-audit 双向验证**(Harness 项目):`audit-redlines.sh` + `audit-ld.sh` 双向(注入违例→FAIL/干净→PASS),CI(ci.yml)+ husky pre-commit 实跑。✅
- ⛔ **G71 平台发布合规预检**(P1 营销内容):见 §2.3,广告法极限词 9 类接输出端,45/45 双向自测。✅(平台特定规则=轻债)

---

## 四、真问题清单与处置(本轮评审产出)

| # | 真问题 | 性质 | 处置 |
|---|---|---|---|
| P-1 | 架构文档 02/06 R-14/R-21 描述滞后(代码已修文档没回填) | 文档债 | ✅ 已回填真实行号(02:45/54/55 · 06:31/33/37/46) |
| P-2 | eval CI 回归门禁完全缺 | 工件缺口 | ✅ 新建 eval-regression.yml |
| P-3 | kappa 阈值 PRD-28(0.4)vs KB gate(0.6)不对齐 | 一致性 | ✅ 对齐 0.6 |
| P-4 | golden custom-062 ID 重复 + 2 specialist 缺配额 | 存量 bug + 缺口 | ✅ 修 ID + 补 2 条 |
| P-5 | 无任何红队脚本/ASR | 工件缺口 | ✅ 实例化 redteam + 拦截率真数据 |
| P-6 | injection-filter 3 漏拦盲区(RH-09/PL-03/II-02) | 防御缺口 | ✅ 盲区1+2 修(91.4%→97.1%)· 盲区3 软肋登记 |
| P-7 | specialist 文案输出端无广告法极限词预检 | 合规缺口(G71) | ✅ output-guardrail 扩极限词接输出端 |

**已知软肋登记(诚实保留,非缺陷)**:
- S-1 II-02"AI系统:"中文前缀间接注入盲区(设计取舍:宁漏勿误报)
- S-2 LLM07 debug 端点 dev 环境返回 systemPrompt(prod 已 FORBIDDEN,仅 protectedProcedure)
- S-3 LLM06 无运行时 RBAC/ABAC(单次调用架构无外部副作用工具,够用)

---

## 五、待真跑项(机制就绪 · 诚实边界 · 不编造)

| 项 | 机制就绪状态 | 真跑触发条件 |
|---|---|---|
| eval kappa≥0.6 真值 | 算法+UI+CI+golden 全就绪 | `pnpm eval:run` + ANTHROPIC_API_KEY + admin 人工打分 30 条 |
| promptfoo 端到端 LLM-ASR<5% | scripts/redteam 全就绪 | `cd scripts/redteam && ./run-redteam.sh`(需 KEY+promptfoo) |

> 二者均 from-0 接地精神:机制/工件/CI/双向自测可接地可证伪,真实数值待真跑回填,**不编造假数值**(编假比没接地更坏)。

---

## 六、出门判定

- **universal 硬门禁(A/B/C/D/E/F/G)**:逐条核验,过(证据见 §三);无未授权访问/无硬编码 secret/OWASP 八项覆盖/RLS 16 表/红队四⛔真跑/合规 N/A 有据。
- **交付能力工件 gates**:eval-suite(CI门禁+阈值+golden 做实,kappa 真值待真跑)/ redteam(拦截率 97.1% 真跑,LLM-ASR 待真跑)/ G71(45/45 双向自测)/ quality-verifiers N/A。
- **待真跑两项**:非门禁失败,是 KEY 环境约束;机制全就绪,经用户拍板"机制做实·真值待真跑"。

**出门结论**:阶段8 universal 评审硬门禁过 + 三交付工件机制做实(真跑出不依赖外部 LLM 的真数据)+ 文档债清 + 红队闭环;**两项 LLM 真值带"待真跑"债出门**(经用户同意·机制就绪非缺失)。→ 可进阶段9 部署上线(LLM 真值在有 KEY 环境/上线前补跑)。

---

## 七、对抗 review 与收敛(评审自身质检)

阶段8 工件完成后,team 模式派 4 路 Sonnet 对抗 review(G71 / injection-redteam / eval / 文档-报告)+ Opus 逐条回源亲核,找出并修复真问题:

| 维度 | 真问题 | 修复 |
|---|---|---|
| **G71 正则** | P1×4:Cat3/Cat4/Cat8/Cat2 正则过宽,误伤 specialist 正常营销输出("最专业的建议/服务一流/全国第一届") | Cat3 改"要求商品名词"·Cat4 删一流·Cat8 后缀必填·Cat2 补排除后缀·补遥遥领先;+17 双向 case(`output-guardrail.ts:52-118`),正向不漏/负向不误 |
| **eval** | P1 SCHEMA.md 失真(VoiceChat残留/70→72)·P1 CI 无KEY无检测·P2 prd-28 FR-7 仍κ0.4 | SCHEMA.md 据实更新 72/PresentationAgent·eval-regression 加 KEY 配置回显 step·prd-28 门禁 4 处→κ≥0.6 |
| **injection/redteam** | P1 II-05 注释失实(说漏拦实被拦,期望3/5)·P1 越权测试标题夸大"返0行"·P1 dataset 漏 Diagnosis/Evolution 配额断言 | 注释/期望修正(间接注入真值 4/5=80%)·标题诚实化(注入层真验/RLS层 E2E)·补 2 配额断言(`dataset-validation.test.ts`) |
| **文档/报告** | P1 BaseSpecialist 行号偏移(:82注释→:85/:95→:100)·P2 manual_rls:49 空行 | 02/06 行号校正 |

**Opus 亲核推翻的误报**:① 旧批 review(cwd 路径错 + 读 KB 模板包/ops-log 旧记录瞎猜"文件不存在/cost-budget 是 Python/api 数字不一致")② ac70a 报"action 版本超前"——核 ci.yml 与 eval-regression 同用 checkout@v5/pnpm@v6/setup-node@v5(项目既定版),非超前。

**收敛后全门**:typecheck 6/6 · api **1222**/0 · admin 64/0 · web 12 pre-existing · audit-redlines 15✅ · audit-ld exit0。

**母题**:① **test 过 ≠ typecheck 绿**——G71 `v[0].label` 测试 45/45 跑过但 tsc 报 6 个 TS2532,Opus 跑全量 typecheck 才抓出(红队 Sonnet 误判 pre-existing,被 git stash 假象骗:stash 把同批 G71 改动一起 stash 了);② **review 也审自己产出**——报告"dataset 断言同步"被 review 揪出过度声称 → 补真断言成真;③ **正则收窄宁漏勿误报**——营销平台误报危害 > 漏报。

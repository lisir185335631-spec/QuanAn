# 阶段7 测试验收报告 · QuanAn

> 2026-06-19 · 阶段6 开发执行出门后内部验收 · 对照 `KB/2-standards/checklists/07-验收清单.md`
> 验收范围:四波次重构(清场/correctness/enrich/规模护栏)+ 二轮对抗复审 + 清残留 + 3 交付能力包(注入过滤/成本熔断/输出合规)。
> 证据规则:每条以命令输出/双向自测/file:line 留痕,非主观判断。

---

## A. 逐条 AC 验证(prd.json 11 story)

| Story | 内容 | passes | 关键验证证据 |
|---|---|---|---|
| US-001 | SpecialistRequest 增 userId 透传 | ✅ | grep `userId:0`=0(生产+fixture)·19 tRPC 站点注 ctx.user.id·rate-limiter userId 隔离单测·typecheck0 |
| US-002 | ContextAssembler 接通 4 Specialist | ✅ | 3 家 invokeLLM 消费 ctx.systemPrompt+3 persona 模板注册(agentId 实证匹配)·AC-4 集成断言(进化档案入 prompt)·AC-5 PII 单测 |
| US-003 | embedding 真接通(D-038) | ✅ | constant-embed 走 OpenAIEmbeddingWorker·functional gateway.embed=0·constant-version 27 测 |
| US-004 | 9 处 catch 补日志 | ✅ | 生产代码无裸空 catch(余 3 处 .catch(()=>void0)=有意 fire-and-forget) |
| US-005 | audit-redlines R-11 检测 | ✅ | R-11 双 facet(systemPrompt=+invokeLLM(_ctx)·双向自测注入→FAIL/删→PASS |
| US-006 | trending_favorites RLS | ✅ | manual_rls.sql ENABLE+account_isolation+admin bypass·R-21 真检测✅ |
| US-007 | G1 token 预算 | ✅ | context-budget MAX110k+CJK 估算·贪心裁剪 budget.test 8/8(超大 stepData→保最近丢最旧) |
| US-008 | G9 真熔断器 | ✅ | circuit-breaker 状态机·complete() 真集成(canAttempt 跳过坏模型)·cb 测试 25 |
| US-009 | G13 演进 100 冻结 | ✅ | EvolutionAgent feedbackCount≥100 guard·g13-freeze 4 测 |
| US-010 | G11 真 LLM Judge | ✅ | isMock=false 真调 gateway(reasoning+rubric)·safeParse 失败抛错·clamp[0,5]·守 D-077 运行时默认 mock |
| US-011 | G12 canary 自动止损 | ✅ | error rate>0.15 AND 样本≥20→executeAutoRollback 回退 stable·BaseSpecialist success=!isFallback(覆盖 VideoAgent 内层 fallback)·字段一致性测试 |

**A 节⛔**:11 story 每条 AC 逐条核(四波次+二轮复审留痕);passes=true 均在硬门验证后设置(非提前)。✅

---

## B. 代码质量检查

- ⛔ **typecheck**:`pnpm typecheck` **6/6 包成功·0 error** ✅
- ⛔ **lint**:`@quanan/schemas lint` 报 **EPERM scandir /Users/return/Desktop**=本地文件系统/sandbox 环境受限(eslint 解析配置向上读父目录),**非代码 lint 错**;CI 环境正常,typecheck 6/6 已兜住类型层。⚠️ 本地不可验·CI 过。
- ⛔ **tests**:`api 1160 passed/0 failed`(+RAG8+PII37+注入35+budget14+output18 等);`web 1248 passed/12 failed`(**12 为 pre-existing 字面锁漂移·git stash 金标准证非本会话回归**) ✅(api 全绿·web 12 存量已登记)
- 测试覆盖:本会话新增 ~150 测(各工件包双向自测)·覆盖未下降。

---

## C. Agent 质量专项验收

- **Prompt 遵循/边界**:各 specialist system prompt 含合规边界(`不承诺具体数字`等·MonetizationAgent/PrivateDomain/Diagnosis boundaries)·G77 output-guardrail 输出端检出承诺性表述软化。
- **RAG Groundedness**:_fetchRag 检索 case/formula/element chunk 注入 ctx Section6·query 已修为用户真实输入(_deriveRagQuery·非 agentId 摆设)。
- ⚠️ **量化指标(工具准确率≥95%/Prompt遵循率≥95%/幻觉率<5%/Golden Dataset)**:需 eval-suite 跑(见 H 节·部分=admin evaluation 体系存在但 Golden Dataset≥20 三元组+Kappa≥0.6+CI 门禁待补)。
- C40 数字零容错:P2=**创作容错**·非零容错→N/A。

---

## D. UI 验收(agent-browser)

- ⚠️ **未跑 agent-browser 端到端**:本会话前端验证靠 realdata 单测(局部注入结构化 data·断言 agent 值≠fallback·BoomGenerate/Step4b/Analysis/VideoAnalysis 各真负向测试 20/20)。
- agent-browser 实跑端到端 UI(打开 URL→操作→断言状态+控制台无 error)**待补**(软项·可阶段7 续或阶段8)。

---

## E. 性能与边界验收

- **Token 预算**:G1 MAX_CONTEXT_TOKENS=110k 贪心裁剪(超预算保最近层)·单请求 token 受控。
- **成本边界**:G78 cost-budget-guard MAX_RUN_COST_USD=0.5·run-scoped 累计超抛 BudgetExceededError→fallback。
- **边界输入**:注入特征(G74)/超长(G1 裁剪)/PII(7 家 mask)已测;空输入各 specialist fallback。
- ⚠️ P95 延迟实测、并发竞争=低并发画像(P8)·未做压测(软项)。

---

## F. 安全基线验收

- ⛔ **PII 脱敏不进 LLM**:**7 家字段级 piiMask**(PrivateDomain/Monetization/Branding/Positioning/Livestream/Diagnosis/Copywriting)+ G77 **输出端**脱敏;4 家主体内容 N/A(脱敏毁分析);PrivateDomain.pii.test 等多测实证(手机/邮箱真脱敏) ✅
- ⛔ **注入/XSS 防护**:G74 注入特征过滤(RAG chunks/stepData 间接注入中和)+ tRPC zod schema 校验入参 + Prisma 参数化(防 SQL 注入) ✅
- ⛔ **API 鉴权**:23 routers protectedProcedure(未授权 401/403)·RLS 三道闸 ✅
- 高风险操作:prompt-version rollback dual approval·canary 自动止损仅回退 stable(安全方向) ✅

---

## G. 回归验证

- ⛔ **未破坏已有功能**:全套 api 1160/0(零回归)·web 12 存量(git stash 证非本会话);四波次每步硬门核零回归。✅
- ⛔ **Prompt 变更回归**:enrich/接通改了 prompt·api 全套测试过(含各 specialist 测试)·realdata 真负向。✅(语义质量量化见 H eval-suite)

---

## H. 交付能力落地验收(画像命中)

| H项 | 画像命中 | 状态 |
|---|---|---|
| H76 eval-suite(Golden Dataset≥20+Kappa≥0.6+CI门禁) | 需效果评估 | ⚠️ **部分**:admin evaluation 体系存在(PRD-28·LLM Judge 闭环·Cohen's kappa)·但 Golden Dataset 规模/CI 回归门禁待补 |
| H77 红队测试(run-redteam.sh ASR<5%·七类攻击) | P3公网+不可信输入 | 📋 **阶段8**:项目 phase_notes 明确红队测试归阶段8(注入特征过滤 G74 已实装为前置防护) |
| H78 quality-verifiers(number/plan_validator) | P2零容错/多步骤规划 | N/A:P2=创作容错·specialist 单步(95/5 无多步规划) |
| H79 Judge 偏差校准(position/length/self-pref) | LLM-as-Judge | ❌ **gap**:G11 LLM Judge 已实装但三类偏差校准未做 |
| H80 媒体质量指标(分辨率/FID/MOS) | P5多模态 | 📋 **轻债**:DALL-E 媒体闸门低优(DALL-E 自带内容策略) |
| H82 失败路径已测(瞬时重试/不可重试降级/补偿) | P10多Agent+副作用 | ⚠️ **部分**:G9 熔断(瞬时重试)+fallback(降级)+G78 budget+G12 止损已测·多步 saga 补偿=单步 specialist 无需 |

---

## 验收结论

- **universal ⛔(A/B/F/G)**:全过(typecheck6/6·api1160/0·PII7家+输出端·注入过滤·鉴权·零回归)。
- **C/D/E 软项**:核心验证靠单测+双向自测;agent-browser 端到端 UI / 量化指标 / 压测待补。
- **H 节⛔ gap**:**H76 eval-suite(部分)+ H79 Judge 偏差校准(gap)** 是阶段7 验收剩余硬项;H77 红队=阶段8(项目既定);H78 N/A;H80/H82 轻债/部分。

**出门判定**:universal 验收过,但 H76/H79 两个画像命中 ⛔ 未达标 → **阶段7 暂不能干净出门**·待补 eval-suite + Judge 偏差校准(或经用户同意记门禁债务带债出门·H77 红队本就阶段8)。

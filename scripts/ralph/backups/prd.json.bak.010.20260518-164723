{
  "project": "QuanQn · 真 OPENAI/Anthropic API key 接入 + 8 Specialist tuning",
  "branchName": "ralph/prd-20-real-llm-integration",
  "description": "PRD-20 真 OPENAI/Anthropic API key 接入 + 8 Specialist real LLM 验证 + cost_log 真接 + userQuota atomic 扣额(money-critical) + TD-79/80/81/82 一并 fix · brownfield LLMGateway 99% 已实施(apps/api/src/workers/llm-gateway/ 5 文件 + BaseSpecialist + 8 Specialist 全 wire) · scope 9 US",
  "userStories": [
    {
      "id": "US-001",
      "title": "ENV validation + LLM client init + 启动时模式 log",
      "description": "作为 QuanQn 后端运维 · 在 apps/api 启动时需明确 ENV 配置正确性 + 当前 LLM 模式(real / fallback)· zod schema 校验 OPENAI_API_KEY + ANTHROPIC_API_KEY 启动时存在性 · 缺则 log warning + 自动走 fallback(D-187 延续)· LLM client init(@anthropic-ai/sdk + openai SDK 已装)· apps/api 启动 log 显示当前模式 · 防配置错误 runtime silent fail",
      "acceptanceCriteria": [
        "新建 apps/api/src/lib/env.ts · export const envSchema = z.object({ OPENAI_API_KEY: z.string().min(1).optional(), ANTHROPIC_API_KEY: z.string().min(1).optional(), LLM_DEFAULT_MODEL: z.string().default('claude-sonnet-4-6'), NODE_ENV: z.enum(['development','production','test']).default('development') }) · export function validateEnv(): Env · 校验失败 console.error + throw · 成功 log mode(real if hasKey else fallback)",
        "新建 apps/api/src/lib/env.test.ts · 5 单测 · (a) 全 key 存在 → mode=real (b) key 缺 → mode=fallback + warning log (c) LLM_DEFAULT_MODEL default 'claude-sonnet-4-6' (d) NODE_ENV default 'development' (e) malformed env → throw",
        "修改 apps/api/src/index.ts · 启动时调 validateEnv() · log 当前 LLM mode(real / fallback) + 各 key 存在状态(✓ / ✗ fallback)· performance < 50ms",
        "修改 .env.example · 明示 OPENAI_API_KEY / ANTHROPIC_API_KEY / LLM_DEFAULT_MODEL · 注释 fallback 行为(无 key 时 BaseSpecialist 自动 fallback mock)· grep 验证 'OPENAI_API_KEY' 命中 ≥ 1",
        "test_command · cd apps/api && pnpm vitest run src/lib/env.test.ts",
        "Typecheck passes(cd apps/api && pnpm typecheck → 0 error)"
      ],
      "depends_on": [],
      "priority": 1,
      "passes": true,
      "notes": "",
      "retryCount": 0,
      "blocked": false,
      "files_to_create": [
        "apps/api/src/lib/env.ts",
        "apps/api/src/lib/env.test.ts"
      ],
      "files_to_modify": [
        "apps/api/src/index.ts",
        ".env.example"
      ],
      "test_command": "cd apps/api && pnpm vitest run src/lib/env.test.ts",
      "size_hint": "small",
      "risk_level": "foundation",
      "anti_patterns": [
        {
          "source_prd": "prd-20-real-llm-integration",
          "source_story": "US-001 (self-injected · env validation 通用风险)",
          "lesson": "ENV validation 必用 zod schema · 不允许手工 process.env.X check · 否则 runtime silent fail",
          "antipattern": "if (!process.env.OPENAI_API_KEY) console.warn('missing'); const key = process.env.OPENAI_API_KEY || 'default'  // 反例 · 手工 check + 默认值",
          "correct": "const env = envSchema.safeParse(process.env); if (!env.success) throw new Error(...); // 正确 · zod schema 严格校验"
        },
        {
          "source_prd": "prd-19-frontend-backend-bridge",
          "source_story": "D-187 BaseSpecialist API_KEY missing fallback",
          "lesson": "缺 key 时不抛 error · 自动 fallback mock + UI [降级] badge · isFallbackable 已含 API_KEY missing(PRD-19)",
          "antipattern": "if (!OPENAI_API_KEY) throw new Error('OPENAI_API_KEY missing')  // 反例 · 抛 error · UI crash",
          "correct": "validateEnv 缺 key 仅 log warning · BaseSpecialist 走 fallback path · isFallbackable += err.message?.includes('API_KEY missing')"
        }
      ]
    },
    {
      "id": "US-002",
      "title": "cost_log 真接 + userQuota atomic 扣额(money-critical)",
      "description": "作为 QuanQn 运营 · 在用户用真 LLM 时需要真 token / cost 数据持久化 + 防超额 budget · BaseSpecialist 4 步模板第 4 步 writeCostLog 真存 prisma costLog(promptTokens + completionTokens Integer + costUsd Decimal(10,6) + model + provider + traceId) · 同时 atomic UPDATE userQuota.dailyUsed += tokens 防超扣 race · 超额返回 QUOTA_EXCEEDED error + UI [配额超限] badge · # $ money-critical · userQuota 用 prisma updateMany WITH WHERE dailyUsed + tokens <= dailyQuota 乐观锁",
      "acceptanceCriteria": [
        "新建 apps/api/src/services/quota/userQuota.service.ts · export async function checkAndDeductQuota(prisma, userId, tokens) · 用 prisma.userQuota.updateMany WITH where: { userId, dailyUsed: { lte: Prisma.raw(`daily_quota - ${tokens}`) } } · updated.count === 0 时返回 { ok: false, reason: 'QUOTA_EXCEEDED', current, quota } · 否则 return { ok: true, remaining }. # $ money-critical: atomic UPDATE 防超扣",
        "新建 apps/api/src/services/quota/__tests__/userQuota.service.test.ts · 单测 (a) 充足 quota → 扣额 + remaining 正确 (b) 已满 quota → return QUOTA_EXCEEDED (c) 新用户 row 不存在 → auto-create plan='free' + dailyQuota=100000 (d) tokens=0 跳过(不扣)(e) ★ 并发 100 用户同时扣 · 最终 dailyUsed === Σ tokens · 不超扣(用 Promise.all 模拟 race)",
        "修改 apps/api/src/specialists/base/BaseSpecialist.ts · 4 步模板第 4 步 · 真接 prisma.costLog.create({ data: { accountId, agentId, modelTier, modelUsed, provider, promptTokens, completionTokens, totalTokens, costUsd: Decimal, durationMs, success, isFallback, traceId } }) · 真 token 来自 invokeLLM 返回 result.usage · # $ money-critical: cost_log 必含所有字段 · costUsd 用 Decimal 不用 Float",
        "修改 apps/api/src/workers/llm-gateway/cost-logger.ts · 若已 stub · 改真接 prisma · 同 BaseSpecialist 模式 · 复用 logger 函数",
        "修改 apps/web/src/components/states/ErrorState.tsx · 加 'quota-exceeded' variant · message '[配额超限] 今日 token 已用尽 · 明日 reset' · onRetry button 隐藏(quota 重置才能用)",
        "test_command · cd apps/api && pnpm vitest run src/services/quota/__tests__/userQuota.service.test.ts",
        "Typecheck passes · concurrent 100 用户单测不超扣"
      ],
      "depends_on": [],
      "priority": 2,
      "passes": true,
      "notes": "",
      "retryCount": 0,
      "blocked": false,
      "files_to_create": [
        "apps/api/src/services/quota/userQuota.service.ts",
        "apps/api/src/services/quota/__tests__/userQuota.service.test.ts"
      ],
      "files_to_modify": [
        "apps/api/src/specialists/base/BaseSpecialist.ts",
        "apps/api/src/workers/llm-gateway/cost-logger.ts",
        "apps/web/src/components/states/ErrorState.tsx"
      ],
      "test_command": "cd apps/api && pnpm vitest run src/services/quota/__tests__/userQuota.service.test.ts",
      "size_hint": "medium",
      "risk_level": "high",
      "anti_patterns": [
        {
          "source_prd": "prd-20-real-llm-integration",
          "source_story": "US-002 (self-injected · money-critical 通用)",
          "lesson": "userQuota.dailyUsed 必 atomic UPDATE WITH WHERE 条件 · 严禁先 SELECT 再 UPDATE(race condition · 并发超扣)",
          "antipattern": "const current = await prisma.userQuota.findUnique(...); if (current.dailyUsed + tokens <= current.dailyQuota) { await prisma.userQuota.update({ data: { dailyUsed: current.dailyUsed + tokens } }) }  // 反例 · 先读后写 · race condition",
          "correct": "await prisma.userQuota.updateMany({ where: { userId, dailyUsed: { lte: Prisma.raw(`daily_quota - ${tokens}`) } }, data: { dailyUsed: { increment: tokens } } })  // 正确 · 单 SQL atomic + WHERE 乐观锁"
        },
        {
          "source_prd": "prd-20-real-llm-integration",
          "source_story": "US-002 (self-injected · cost_log 精度)",
          "lesson": "cost_log token 必 Integer · costUsd 必 Decimal(10,6) · 严禁 Float(精度漂移)· promptTokens + completionTokens 必单独存(不只存 totalTokens)",
          "antipattern": "promptTokens: parseFloat(usage.input_tokens), costUsd: 0.000123  // 反例 · Float + 缺 completionTokens",
          "correct": "promptTokens: parseInt(usage.input_tokens, 10), completionTokens: parseInt(usage.output_tokens, 10), totalTokens: promptTokens + completionTokens, costUsd: new Decimal(model.pricing * totalTokens)  // 正确 · Int + Decimal"
        },
        {
          "source_prd": "prd-19-frontend-backend-bridge",
          "source_story": "D-187 BaseSpecialist isFallbackable",
          "lesson": "QuotaExceededError 也要加 isFallbackable · 配额超时自动 fallback mock(可选)· 否则用户 UI 直接 crash",
          "antipattern": "if (quotaCheck.ok === false) throw new TRPCError({ code: 'RESOURCE_EXHAUSTED' })  // 反例 · 真硬抛 · UI crash",
          "correct": "if (quotaCheck.ok === false) throw new TRPCError({ code: 'RESOURCE_EXHAUSTED', message: 'QUOTA_EXCEEDED' })  // 同样抛 · 但 frontend ErrorState 'quota-exceeded' variant 优雅渲染"
        }
      ]
    },
    {
      "id": "US-003",
      "title": "PositioningAgent 真 LLM(industry + execution mode · for step1 + step4)",
      "description": "作为 QuanQn 用户 · 在 Step1 选行业 + Step4 填执行参数 · 真接 PositioningAgent 真 LLM · PositioningAgent.invokeLLM 真接 Anthropic Claude(或 OpenAI · model_tier='reasoning')· 真 system prompt + responseFormat schema 验证 · 真 result 存 DB + UI render · 同时验证 fallback mock vs 真 LLM 输出格式对齐(防 Schema drift · safeParse 严格匹配 outputSchema)",
      "acceptanceCriteria": [
        "修改 apps/api/src/specialists/PositioningAgent.ts · invokeLLM real call(this.llmGateway.complete 已 wire · brownfield) · system prompt tune(per mode: industry / execution)· 真 model 从 ctx 读 actual model(SHIELD REJ-003 · 不 hardcode 'claude-sonnet-4-6')· 真 token usage 真返回(供 BaseSpecialist step 4 writeCostLog 真存)",
        "新建 apps/api/src/specialists/__tests__/PositioningAgent.real-llm.test.ts · describe.skipIf(!process.env.OPENAI_API_KEY && !process.env.ANTHROPIC_API_KEY) · 2 单测 (a) industry mode result.market_analysis/competition_level/positioning_suggestion 都 valid (b) execution mode result.daily_tasks/weekly_milestones/stage_kpis 都 valid · 验证 result.isFallback=false + modelUsed match /claude|gpt/ + tokensUsed.total > 0 + durationMs < 60000",
        "Schema drift 防御 · 真 LLM 输出 schema 严格匹配 fallback mock 输出格式 · toMatchObject 验证关键字段 + outputSchema.safeParse(若 schema drift · 抛 SchemaValidationError → isFallbackable → retry 1 → fallback mock)",
        "Use agent-browser 打开 http://localhost:5173/step/1(若 OPENAI_API_KEY 在 env)· 选行业 + 提交 · 等 LoadingState '正在分析行业 · 请稍候 ...' → 出 result 渲染 · UI 0 [降级] badge(真 LLM 跑通) · console 0 error",
        "Verify cost_log 真接 · prisma.costLog.findFirst({ where: { agentId: 'PositioningAgent', accountId, traceId } }) 非 null · promptTokens > 0 + completionTokens > 0 + costUsd > 0",
        "test_command · OPENAI_API_KEY=$ANTHROPIC_API_KEY cd apps/api && pnpm vitest run src/specialists/__tests__/PositioningAgent.real-llm.test.ts",
        "Typecheck passes · zero-regression 旧 PositioningAgent.test.ts 不动 + 全 PASS"
      ],
      "depends_on": [
        "US-001",
        "US-002"
      ],
      "priority": 3,
      "passes": true,
      "notes": "",
      "retryCount": 0,
      "blocked": false,
      "files_to_create": [
        "apps/api/src/specialists/__tests__/PositioningAgent.real-llm.test.ts"
      ],
      "files_to_modify": [
        "apps/api/src/specialists/PositioningAgent.ts"
      ],
      "test_command": "cd apps/api && pnpm vitest run src/specialists/__tests__/PositioningAgent.real-llm.test.ts",
      "size_hint": "medium",
      "risk_level": "high",
      "anti_patterns": [
        {
          "source_prd": "prd-4-p3-ip-flow-9-steps",
          "source_story": "US-007 (TopicAgent hardcode model)",
          "lesson": "Specialist invokeLLM 严禁 hardcode model name(如 'claude-sonnet-4-6')· 必从 ctx / config 读 actual model · SHIELD REJ-003/D-019 · cost_log 也用这个 actual model 字段",
          "antipattern": "return { result: ..., modelUsed: 'claude-sonnet-4-6', tokensUsed: ... }  // 反例 · hardcode model name",
          "correct": "return { result: ..., modelUsed: ctx.actualModel ?? this.config.execution.model_tier, tokensUsed: response.usage }  // 正确 · 从 ctx / config 读"
        },
        {
          "source_prd": "prd-20-real-llm-integration",
          "source_story": "US-003 (self-injected · Schema drift)",
          "lesson": "真 LLM 接入后输出 schema 必须严格匹配 fallback mock 输出格式 · 否则 frontend Step{N}OutputContent render 时 undefined / 字段 drift · UI crash",
          "antipattern": "// fallback mock 返回 { positioning_suggestion: 'X' } · 真 LLM 返回 { suggestion: 'X' }  // 反例 · schema drift",
          "correct": "outputSchema.safeParse(rawResponse) 严格 + responseFormat 双 schema 策略(LLM 用 BaseSchema · post-validate OutputSchema)· 保证字段对齐"
        },
        {
          "source_prd": "prd-19-frontend-backend-bridge",
          "source_story": "TD-82 反例(real LLM SSE)",
          "lesson": "真 LLM 跟 fallback mock 行为差异 · 测试必须 skipIf 区分 · 否则 CI 强跑 + cost 爆炸 / 跑失败假 fail",
          "antipattern": "describe('PositioningAgent', () => { it('真 LLM', async () => { ... })  // 反例 · CI 无 key 跑 → ENV_NOT_SET timeout",
          "correct": "describe.skipIf(!process.env.OPENAI_API_KEY && !process.env.ANTHROPIC_API_KEY)('PositioningAgent real LLM', () => { ... })  // 正确 · 无 key 时 SKIP"
        }
      ]
    },
    {
      "id": "US-004",
      "title": "BrandingAgent 真 LLM(packaging + persona mode · for step3 + step3b)",
      "description": "作为 QuanQn 用户 · 在 Step3 IP 定位 + Step3b 人设 · 真接 BrandingAgent · BrandingAgent.invokeLLM 真接 LLM · packaging mode(step3)+ persona mode(step3b)· system prompt 双 mode 严格区分 + responseFormat schema 各自验证 · cost_log 真存",
      "acceptanceCriteria": [
        "修改 apps/api/src/specialists/BrandingAgent.ts · invokeLLM real call · packaging vs persona mode 严格区分 · 各自 system prompt + responseFormat schema · 不 hardcode model",
        "新建 apps/api/src/specialists/__tests__/BrandingAgent.real-llm.test.ts · describe.skipIf · 2 单测 packaging + persona mode · 验证 result schema 严格对齐 fallback mock + isFallback=false + tokensUsed > 0",
        "Schema drift 防御 · 跟 US-003 同模式 · outputSchema.safeParse 严格 + double schema strategy",
        "Verify cost_log 真接 · BrandingAgent agentId 真存 · packaging/persona agentMode 字段也真存",
        "test_command · cd apps/api && pnpm vitest run src/specialists/__tests__/BrandingAgent.real-llm.test.ts(若 key 在 env)",
        "Typecheck passes · zero-regression"
      ],
      "depends_on": [
        "US-001",
        "US-002"
      ],
      "priority": 4,
      "passes": true,
      "notes": "",
      "retryCount": 0,
      "blocked": false,
      "files_to_create": [
        "apps/api/src/specialists/__tests__/BrandingAgent.real-llm.test.ts"
      ],
      "files_to_modify": [
        "apps/api/src/specialists/BrandingAgent.ts"
      ],
      "test_command": "cd apps/api && pnpm vitest run src/specialists/__tests__/BrandingAgent.real-llm.test.ts",
      "size_hint": "medium",
      "risk_level": "high",
      "anti_patterns": [
        {
          "source_prd": "prd-4-p3-ip-flow-9-steps",
          "source_story": "US-007 (TopicAgent hardcode model)",
          "lesson": "Specialist invokeLLM 严禁 hardcode model name · 必从 ctx / config 读 actual model · SHIELD REJ-003",
          "antipattern": "modelUsed: 'claude-sonnet-4-6'  // 反例",
          "correct": "modelUsed: ctx.actualModel ?? this.config.execution.model_tier  // 正确"
        },
        {
          "source_prd": "prd-20-real-llm-integration",
          "source_story": "US-003 (Schema drift)",
          "lesson": "packaging vs persona 双 mode 必各自独立 system prompt + outputSchema · 不允许混用",
          "antipattern": "system prompt = 'You are branding expert' (单 prompt 双 mode 用)  // 反例 · mode 行为不区分",
          "correct": "systemPrompt = mode === 'packaging' ? '...账号包装...' : '...人设方案...'  // 正确"
        },
        {
          "source_prd": "prd-20-real-llm-integration",
          "source_story": "US-003 (test skipIf)",
          "lesson": "real-llm.test.ts 必 describe.skipIf 无 key 时 SKIP · CI 默认 skip",
          "antipattern": "describe('BrandingAgent real LLM', () => ...)  // 反例 · CI 强跑",
          "correct": "describe.skipIf(skipIfNoKey)('BrandingAgent real LLM', () => ...)"
        }
      ]
    },
    {
      "id": "US-005",
      "title": "MonetizationAgent + VideoAgent 真 LLM(for step4b + step6)",
      "description": "作为 QuanQn 用户 · 在 Step4b 变现规划 + Step6 拍摄计划 · 真接 2 Agent · MonetizationAgent(3 阶梯 + 收入结构 + 案例 输出 schema)+ VideoAgent shooting mode(8 列分镜表 输出 schema · 复杂嵌套)· 防 hardcode model + Schema drift",
      "acceptanceCriteria": [
        "修改 apps/api/src/specialists/MonetizationAgent.ts · invokeLLM real call · 3 阶梯 schema(0→90万/100万→1000万/1000万→1亿)严格验证 + 收入结构 + 案例 array 嵌套 schema",
        "修改 apps/api/src/specialists/VideoAgent.ts · invokeLLM real call · shooting mode 主(production/acquisition mode 暂不动) · 8 列分镜表 schema(时长/场景/景别/角度/运镜/情绪/台词/动作)严格验证",
        "新建 2 *.real-llm.test.ts · describe.skipIf · 各 1 单测 验证 schema 严格对齐 fallback mock",
        "Schema drift 防御 · MonetizationAgent.threeStages.length === 3 · VideoAgent storyboard 8 列 column 名严格匹配 · safeParse 严格",
        "Verify cost_log 真接 · 2 Agent agentId 真存 + tokens > 0",
        "test_command · cd apps/api && pnpm vitest run src/specialists/__tests__/MonetizationAgent.real-llm.test.ts src/specialists/__tests__/VideoAgent.real-llm.test.ts(若 key 在 env)",
        "Typecheck passes · zero-regression"
      ],
      "depends_on": [
        "US-001",
        "US-002"
      ],
      "priority": 5,
      "passes": true,
      "notes": "",
      "retryCount": 0,
      "blocked": false,
      "files_to_create": [
        "apps/api/src/specialists/__tests__/MonetizationAgent.real-llm.test.ts",
        "apps/api/src/specialists/__tests__/VideoAgent.real-llm.test.ts"
      ],
      "files_to_modify": [
        "apps/api/src/specialists/MonetizationAgent.ts",
        "apps/api/src/specialists/VideoAgent.ts"
      ],
      "test_command": "cd apps/api && pnpm vitest run src/specialists/__tests__/MonetizationAgent.real-llm.test.ts src/specialists/__tests__/VideoAgent.real-llm.test.ts",
      "size_hint": "medium-large",
      "risk_level": "high",
      "anti_patterns": [
        {
          "source_prd": "prd-4-p3-ip-flow-9-steps",
          "source_story": "US-007",
          "lesson": "Specialist 严禁 hardcode model · 必从 ctx 读 SHIELD REJ-003",
          "antipattern": "modelUsed: 'claude-sonnet-4-6'  // 反例",
          "correct": "modelUsed: ctx.actualModel ?? this.config.execution.model_tier"
        },
        {
          "source_prd": "prd-6-p5-video-modules",
          "source_story": "US-002 (Storyboard schema)",
          "lesson": "VideoAgent storyboard schema 必 array 8 列严格 + 字段名严格 · 真 LLM 漂移可能(中文 vs 英文 column name)",
          "antipattern": "storyboard: [{ '镜头号': 1, ... }]  // 反例 · 中文 key",
          "correct": "storyboard: [{ shot_id: 1, scene: '...', shot_type: '...' }] · post-process Chinese label render UI 显示"
        },
        {
          "source_prd": "prd-20-real-llm-integration",
          "source_story": "US-003",
          "lesson": "test skipIf 无 key SKIP · CI 不强跑",
          "antipattern": "describe('Agent real LLM', () => ...)  // 反例",
          "correct": "describe.skipIf(skipIfNoKey)(...)"
        }
      ]
    },
    {
      "id": "US-006",
      "title": "TopicAgent SSE 真 LLM(5 chunks · TD-82 自然 resolved 期望)",
      "description": "作为 QuanQn 用户 · 在 Step5 选 1 类 + 提交 · 真 SSE 流接 TopicAgent · 真 5 chunks 渐进出 5 类 100 选题 · TopicAgent.invokeLLM 真走 llmGateway.stream() SSE · 真 5 chunks 触发(per category) · 跟 fallback mock(1 次性返回)区分 · ★ TD-82 自然 resolved 期望(PRD-18 test3 fallback 不模拟 5 chunks · 真 LLM 接入后会自然 5 chunks · 5 Tab 渐进 visible · 真 SSE 流出)",
      "acceptanceCriteria": [
        "修改 apps/api/src/specialists/TopicAgent.ts · invokeLLM real SSE call · 5 chunks 渐进 yield(per category)· 真 model 从 ctx 读 · 真 stream meta chunk { type: 'meta', meta: { model: actualModel } } 首 chunk(< 3s)· cost_log 真存 stream 累积 token",
        "新建 apps/api/src/specialists/__tests__/TopicAgent.real-llm.test.ts · describe.skipIf · 1 SSE chunks 单测 · for await chunk of topicAgent.executeStream(...) · doneChunks.length === 5(per 5 category 自然触发)· doneChunks.map(c => c.category).sort() === ['case', 'cognition', 'monetize', 'persona', 'traffic']",
        "Schema drift 防御 · 真 5 chunks 每个 category 含 20 选题 · result.topics.length === 20 + outputSchema.safeParse 严格",
        "Verify cost_log 真接 · stream 总 token > 5 chunks 累积 · costUsd 真 > 0",
        "★ TD-82 验证准备 · 真 LLM 跑(若 OPENAI_API_KEY 存在)· playwright manual test playwright test e2e/prd-18-step-4-5-6-7-8.spec.ts test3 5 Tab 渐进 visible · TD-82 自然 resolved(若仍 fail · 留 US-008 B/C 选项 fix)",
        "test_command · cd apps/api && pnpm vitest run src/specialists/__tests__/TopicAgent.real-llm.test.ts(若 key 在 env)",
        "Typecheck passes · zero-regression"
      ],
      "depends_on": [
        "US-001",
        "US-002"
      ],
      "priority": 6,
      "passes": true,
      "notes": "",
      "retryCount": 0,
      "blocked": false,
      "files_to_create": [
        "apps/api/src/specialists/__tests__/TopicAgent.real-llm.test.ts"
      ],
      "files_to_modify": [
        "apps/api/src/specialists/TopicAgent.ts"
      ],
      "test_command": "cd apps/api && pnpm vitest run src/specialists/__tests__/TopicAgent.real-llm.test.ts",
      "size_hint": "large",
      "risk_level": "high",
      "anti_patterns": [
        {
          "source_prd": "prd-19-frontend-backend-bridge",
          "source_story": "TD-82 (PRD-18 test3 SSE 不模拟 chunks)",
          "lesson": "真 LLM 接入后 SSE 必须真 yield 5 chunks(per category)· fallback mock 1 次性返回 100 选题 ≠ 真 5 chunks · TD-82 自然 resolved 需 5 chunks 真触发",
          "antipattern": "// fallback mock 1 次性返回 · 真 LLM 也 1 次性返回(用 await invokeLLM 不 streaming)  // 反例 · 不模拟 SSE chunks",
          "correct": "for await chunk of this.llmGateway.stream(...) { yield { type: 'meta'/'delta'/'done', ... }; }  // 正确 · 真 streaming yield 5 done chunks"
        },
        {
          "source_prd": "prd-4-p3-ip-flow-9-steps",
          "source_story": "US-007 (TopicAgent hardcode + meta chunk)",
          "lesson": "SSE 模式必首 chunk { type: 'meta', meta: { model: actualModel } } < 3s · cost_log.modelUsed 反映真 model · D-019 闭环",
          "antipattern": "yield { type: 'done', result }  // 反例 · 无 meta chunk · 客户端不知 model",
          "correct": "yield { type: 'meta', meta: { model: actualModel } }; ... yield { type: 'done', result }  // 正确"
        },
        {
          "source_prd": "prd-20-real-llm-integration",
          "source_story": "US-003 (test skipIf)",
          "lesson": "real-llm test 必 skipIf · CI 默认 skip",
          "antipattern": "describe('TopicAgent SSE real LLM', () => ...)  // 反例",
          "correct": "describe.skipIf(skipIfNoKey)('TopicAgent SSE real LLM', () => ...)"
        }
      ]
    },
    {
      "id": "US-007",
      "title": "CopywritingAgent + LivestreamAgent 真 LLM(for step7 + step8 2 子功能)",
      "description": "作为 QuanQn 用户 · 在 Step7 文案生成 + Step8 直播策划 2 子功能 · 真接 2 Agent · CopywritingAgent.invokeLLM 真 SSE call(step7 mode · 长 markdown 输出 + history 自动写)+ LivestreamAgent.invokeLLM 真 call(generate_plan + optimize_script 双 sub_function · discriminator 严守 PRD-19 §11.11.4)",
      "acceptanceCriteria": [
        "修改 apps/api/src/specialists/CopywritingAgent.ts · invokeLLM real SSE call(step7 mode · 长 markdown · stream yield)· 真 model 从 ctx 读 · history 自动写 prisma.history.create(已 wire in stepData router · brownfield)",
        "修改 apps/api/src/specialists/LivestreamAgent.ts · invokeLLM real call · 2 sub_function 区分(generate_plan / optimize_script)· system prompt 双 sub_function 严格区分 + responseFormat schema 各自(6 模块 vs 2 InfoCard)· userInput.experience enum(新手/有经验/资深) zod validate",
        "新建 2 *.real-llm.test.ts · describe.skipIf · CopywritingAgent step7 mode SSE chunks 单测 + LivestreamAgent 2 sub_function 各 1 单测 · 验证 schema 严格对齐 fallback mock",
        "Schema drift 防御 · CopywritingAgent markdown 输出 + body.text 嵌套 · LivestreamAgent 6 模块 / 2 InfoCard schema 严格",
        "Verify cost_log 真接 · 2 Agent agentId 真存 · CopywritingAgent agentMode='step7' + LivestreamAgent agentMode='generate_plan'/'optimize_script'",
        "test_command · cd apps/api && pnpm vitest run src/specialists/__tests__/CopywritingAgent.real-llm.test.ts src/specialists/__tests__/LivestreamAgent.real-llm.test.ts(若 key 在 env)",
        "Typecheck passes · zero-regression"
      ],
      "depends_on": [
        "US-001",
        "US-002"
      ],
      "priority": 7,
      "passes": false,
      "notes": "[TD-HINT] 同 US-003~006 · 防 hardcode model + Schema drift · LivestreamAgent sub_function discriminator 严守 PRD-19 §11.11.4(LS load + dbQuery sync 双重 check)",
      "retryCount": 0,
      "blocked": false,
      "files_to_create": [
        "apps/api/src/specialists/__tests__/CopywritingAgent.real-llm.test.ts",
        "apps/api/src/specialists/__tests__/LivestreamAgent.real-llm.test.ts"
      ],
      "files_to_modify": [
        "apps/api/src/specialists/CopywritingAgent.ts",
        "apps/api/src/specialists/LivestreamAgent.ts"
      ],
      "test_command": "cd apps/api && pnpm vitest run src/specialists/__tests__/CopywritingAgent.real-llm.test.ts src/specialists/__tests__/LivestreamAgent.real-llm.test.ts",
      "size_hint": "medium-large",
      "risk_level": "high",
      "anti_patterns": [
        {
          "source_prd": "prd-4-p3-ip-flow-9-steps",
          "source_story": "US-009 (CopywritingAgent SSE meta chunk)",
          "lesson": "CopywritingAgent SSE 模式必 stream meta chunk · 不用 invokeLLM 直返 · 否则 cost_log 缺 actual model",
          "antipattern": "return { result: markdown, modelUsed: 'claude-sonnet-4-6' }  // 反例",
          "correct": "yield { type: 'meta', meta: { model: actualModel } }; for await chunk of stream { yield ... }"
        },
        {
          "source_prd": "prd-19-frontend-backend-bridge",
          "source_story": "§11.11.4 sub_function discriminator",
          "lesson": "LivestreamAgent 2 sub_function 必各自独立 system prompt + outputSchema · 不允许混用 prompt · 否则 generate_plan / optimize_script 输出可能 cross-talk",
          "antipattern": "systemPrompt = 'You are livestream expert' (单 prompt 双 sub_function 用)  // 反例",
          "correct": "systemPrompt = sub_function === 'generate_plan' ? '...直播方案...' : '...话术优化...'  // 正确"
        },
        {
          "source_prd": "prd-20-real-llm-integration",
          "source_story": "US-003",
          "lesson": "test skipIf 无 key SKIP",
          "antipattern": "describe('Agent real LLM', () => ...)  // 反例",
          "correct": "describe.skipIf(skipIfNoKey)(...)"
        }
      ]
    },
    {
      "id": "US-008",
      "title": "E2E 双跑(fallback default + manual/CI weekly real LLM)+ TD-79/80/81/82 一并 maintenance fix",
      "description": "作为 QuanQn 维护者 · 需要双跑 E2E · 默认 fallback(快 + 无 cost)+ manual/CI weekly 真 LLM(完整端到端)· playwright spec 双跑配置 · E2E_REAL_LLM=1 env 控制 · 真 LLM 跑 PRD-18 test3 应自然 PASS(TD-82 resolved)· PRD-15~19 旧 e2e zero-regression 全 PASS · TD-79/80/81 一并 maintenance fix",
      "acceptanceCriteria": [
        "新建 apps/web/e2e/prd-20-real-llm.spec.ts · test.describe(`PRD-20 ${REAL_LLM ? 'real LLM' : 'fallback'}`) · 4 tests · (a) Step5 SSE 5 tabs 真触发(TD-82 验证)· 真 LLM 5 chunks 5 tab 渐进 visible (b) 真 LLM cost_log 真存 verify (c) userQuota atomic 扣额验证 (d) zero-regression vitest 181+ + 旧 PRD-15~19 e2e 全 PASS",
        "新建 .github/workflows/e2e-weekly-real-llm.yml(若选 GitHub CI · 否则文档说明)· weekly cron · OPENAI_API_KEY secret · 真 LLM full e2e run · cost ~$1/run · monthly ~$5",
        "修改 apps/web/playwright.config.ts · if env E2E_REAL_LLM=1 设 timeout 90s + retries 2(真 LLM 慢 + 偶尔超时容忍)",
        "TD-79 fix(决策 B 推荐 · 升级 source-of-truth)· apps/web/src/lib/constants/industries.ts 保留新 label STEP1_CTA_LABEL='生成行业洞察' + STEP1_NEXT_LABEL='进入 IP 定位 →' · grep 验证常量字面 ≥ 1",
        "TD-80 fix · industries.ts 新增 STEP1_RESULT_H2='行业洞察报告' + STEP1_RESULT_H3_3=[市场分析,竞争程度,定位建议] as const · step3.ts 新增 STEP3_RESULT_H2='账号包装方案' + STEP3_OPTIMIZE_CONFIRM_BUTTON='确认优化' · step3b.ts 新增 STEP3B_RESULT_H2='专属人设方案' · Step1/3/3b.tsx 改用常量 · grep 验证 hardcode 0 hit",
        "TD-81 fix · Step5TopicGrid.tsx:75-76 删 backward compat double setItem 'acc_step5_selected_topic' · 只保留 namespaced key · grep 验证 'acc_step5_selected_topic' 0 hit",
        "TD-82 验证 · 真 LLM 跑(若 OPENAI_API_KEY 存在)· playwright test e2e/prd-18-step-4-5-6-7-8.spec.ts test3 5 Tab 渐进 visible · 若仍 fail · B/C 选项 fix(B Step5 fallback 模拟 5 chunks 或 C TopicAgent fallback 改 5 chunks · 留 1 选项 implement)",
        ".agents/tech-debt.json TD-79/80/81/82 status=resolved + resolved_in_prd='prd-20-real-llm-integration' + resolved_at + resolved_notes · commit message tag [TD-79 resolved] [TD-80 resolved] [TD-81 resolved] [TD-82 resolved]",
        "Use agent-browser test E2E 默认 fallback · pnpm playwright test e2e/prd-20-real-llm.spec.ts · 4 tests PASS",
        "Typecheck passes · zero-regression vitest 181+ + 旧 e2e 全 PASS"
      ],
      "depends_on": [
        "US-003",
        "US-004",
        "US-005",
        "US-006",
        "US-007"
      ],
      "priority": 8,
      "passes": false,
      "notes": "[TD-HINT] US-008 主要 scope 是 4 TD 一并 fix · 严守 fix_hint 模板 · 不超出 scope",
      "retryCount": 0,
      "blocked": false,
      "files_to_create": [
        "apps/web/e2e/prd-20-real-llm.spec.ts",
        ".github/workflows/e2e-weekly-real-llm.yml"
      ],
      "files_to_modify": [
        "apps/web/playwright.config.ts",
        "apps/web/src/lib/constants/industries.ts",
        "apps/web/src/lib/constants/step3.ts",
        "apps/web/src/lib/constants/step3b.ts",
        "apps/web/src/pages/step/Step1.tsx",
        "apps/web/src/pages/step/Step3.tsx",
        "apps/web/src/pages/step/Step3b.tsx",
        "apps/web/src/components/step5/Step5TopicGrid.tsx",
        "apps/web/e2e/prd-18-step-4-5-6-7-8.spec.ts",
        ".agents/tech-debt.json"
      ],
      "test_command": "cd apps/web && pnpm playwright test e2e/prd-20-real-llm.spec.ts",
      "size_hint": "large",
      "risk_level": "high",
      "anti_patterns": [
        {
          "source_prd": "prd-19-frontend-backend-bridge",
          "source_story": "TD-82 (PRD-18 test3 pre-existing fail)",
          "lesson": "PRD-18 test3 5 Tab 期望真 SSE 5 chunks · 真 LLM 接入后自然 PASS(若仍 fail · 用 test.skip(!OPENAI_API_KEY, ...) 标记 manual only 或 fallback 模拟 5 chunks)",
          "antipattern": "// PRD-18 test3 完全删除 · 假装通过  // 反例 · 丢失 test 覆盖",
          "correct": "PRD-18 test3 加 test.skip(!process.env.OPENAI_API_KEY, 'real LLM 真 SSE 5 chunks 测') 或修 fallback path 模拟 chunks"
        },
        {
          "source_prd": "prd-19-frontend-backend-bridge",
          "source_story": "TD-79 (STEP1_CTA_LABEL 'mock' → '真 Agent' label evolution)",
          "lesson": "label evolution 决策 B · 保留新 label(配真 Agent 语义)· 更新 source-of-truth + 反例库",
          "antipattern": "// 决策 A · 回滚 STEP1_CTA_LABEL='确认并进入下一步'  // 反例 · mock label 在真 Agent 接入后语义偏差",
          "correct": "保留 STEP1_CTA_LABEL='生成行业洞察' · grep 验证 + 反例库注入 'mock label → 真 Agent label evolution'"
        },
        {
          "source_prd": "prd-19-frontend-backend-bridge",
          "source_story": "TD-81 (Step5 backward compat dead code)",
          "lesson": "Step7 已 migrate readOtherStep selected_topic · Step5TopicGrid backward compat 双写是 dead code · 安全删",
          "antipattern": "// 保留 backward compat 双写  // 反例 · dead code 累加",
          "correct": "Step5TopicGrid.tsx:75-76 删 · 只保留 namespaced key 写入"
        }
      ]
    },
    {
      "id": "US-009",
      "title": "verify-prd-20.sh 40+ 检查 + zero-regression + Specialist tuning baseline 文档",
      "description": "作为 QuanQn 维护者 · 需要可重复 bash script 验证 PRD-20 全部交付物 + Specialist tuning baseline 留痕 · scripts/verify-prd-20.sh 40+ 检查项 · 含 §1 ENV validation + §2 LLM SDK 真接 + §3 cost_log 真数据 grep + §4 userQuota atomic + §5 8 Specialist real LLM test 全 PASS(若 key)+ §6 TD-79/80/81/82 fix 验证 + §7 zero-regression + §8 D4=B/D3=A/LD-009 严守 + §9 Specialist tuning baseline 文档",
      "acceptanceCriteria": [
        "新建 scripts/verify-prd-20.sh(chmod +x)· 40+ 检查项 · §1 ENV validation(3 项) · §2 LLM SDK 真接(3 项) · §3 cost_log 真数据 + atomic UPDATE(4 项 money-critical) · §4 8 Specialist real LLM test PASS if key(8 项 · SKIP if no key) · §5 TD-79/80/81/82 fix(4 项) · §6 zero-regression typecheck+vitest 181+(3 项) · §7 D4=B/D3=A/LD-009 严守(3 项) · §8 Specialist tuning baseline doc(1 项)",
        "新建 .agents/specialist-tuning-baseline.md · 8 Specialist baseline 文档 · 每 Agent · model_tier(reasoning) / avg input_tokens / avg output_tokens / avg cost USD / 调优历史 / 跟 fallback mock 输出对比表 · PRD-21+ Specialist 优化有对照基线",
        ".agents/tech-debt.json TD-79/80/81/82 全 status=resolved · resolved_in_prd='prd-20-real-llm-integration' · resolved_at + resolved_notes 含 fix evidence(commit + verify §X PASS)",
        "verify script SKIP 路径 · OPENAI_API_KEY 不存在时 §3.4/§4/§5.4 echo SKIP(不 fail · exit 0)· 默认 fallback mode 跑 §1~§3.3/§5.1~5.3/§6~§9 全部 · ≥ 25 项 PASS",
        "verify script 真跑 · bash scripts/verify-prd-20.sh · 40/40 PASS(若 key)/ 25/40 PASS + 15 SKIP(若无 key)· exit 0 · CI 友好",
        "commit message tag [TD-79 resolved] [TD-80 resolved] [TD-81 resolved] [TD-82 resolved]",
        "Typecheck passes · zero-regression vitest 181+ 全 PASS"
      ],
      "depends_on": [
        "US-008"
      ],
      "priority": 9,
      "passes": false,
      "notes": "[TD-HINT] US-009 主任务即 TD-79/80/81/82 全 resolved + Specialist tuning baseline 文档化(为 PRD-21+ Specialist 优化留对照)",
      "retryCount": 0,
      "blocked": false,
      "files_to_create": [
        "scripts/verify-prd-20.sh",
        ".agents/specialist-tuning-baseline.md"
      ],
      "files_to_modify": [
        ".agents/tech-debt.json"
      ],
      "test_command": "bash scripts/verify-prd-20.sh",
      "size_hint": "medium",
      "risk_level": "medium",
      "anti_patterns": [
        {
          "source_prd": "prd-18-step-4-4b-5-6-7-8",
          "source_story": "US-013 (verify script 模板)",
          "lesson": "verify script 必 chmod +x · 40+ 检查项分多 § · 任一失败 exit non-zero · SKIP 不算 fail",
          "antipattern": "echo '[WARN] 跳过检查 X · 待修复'; exit 0  // 反例 · silent skip",
          "correct": "[ '$HIT' = '0' ] && ok '...' || fail '...'; exit $FAIL  // 正确 · 严格 exit code · SKIP 路径明示"
        }
      ]
    }
  ]
}
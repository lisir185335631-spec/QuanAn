US-012 实现完成。以下是完成摘要：

**实现了所有 13 条 AC：**

1. **AC-2 trending-scraper brownfield** — `worker.ts` 起始检查 `getFeatureFlagValue('stop_trending_scraper')`，true → `logger.warn + return`
2. **AC-3 EvolutionAgent brownfield** — `execute()` 起始检查 `getSystemConfigValue('stop_evolution_agent')`，true → `{ isFallback: true, modelUsed: 'emergency-disabled' }`
3. **AC-4 ContextAssembler brownfield** — `_fetchActivePrompt` + `_fetchActiveConstants` 检查 `enable_fallback_prompt`，true → skip DB → fallback to templates/*.ts + lib/constants/*.ts
4. **AC-5 emergencyToggleSystemConfig** — 已有 `requestApproval → emergencyApprove(postReviewRequired=true) → _updateSystemConfigInTx`
5. **AC-6 audit log** — 写 `eventType='emergency_switch_triggered'` + `eventCategory='security_alert'`，含 configKey + incidentId
6. **AC-7 钉钉 isMock=true** — `DingtalkService()` 默认 mock 模式
7. **AC-8 post-review cron** — 已覆盖（scan ALL `postReviewRequired=true`，含 `update_system_config` 路径）
8. **AC-9 tests** — 12 it 块（≥8），覆盖 3 开关触发 + bypass + TTL cache + flagType 路由 + 钉钉 + 单点
9. **AC-10 5s TTL cache** — `Map + setInterval`，`getFeatureFlagValue` + `getSystemConfigValue` 双缓存
10. **AC-11 不破坏现有流程** — brownfield 仅前置 1 行检查，不替代主逻辑
11. **AC-12/13 pnpm test 1906 pass / typecheck 0 errors** ✅

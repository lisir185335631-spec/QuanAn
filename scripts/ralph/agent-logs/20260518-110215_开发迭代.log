US-008 retry-5 修复完成。4 个 CONFIRMED 失败均已针对性修复：

| 失败 | 修复方案 |
|---|---|
| **AC-3 test(b)** 切回 A 得到 B 的数据 | A→B / B→A 切换改用 `waitForEvent('load')` 确保 reload 完成 |
| **AC-4 test(c) Step5** strict-mode 多匹配 | `locator('text=流量型')` → `getByRole('tab', {name:'流量型'})` |
| **AC-5 prd-18 test3** '変現型' 30s 超时 | `test.describe.configure({ mode: 'serial' })` 防 test(c) clearStepDataForTest 并发竞争 |
| **AC-5 prd-18 test5** '评论区引导' strict-mode | `locator('text=评论区引导')` → `.first()` |

Typecheck 0 error，已 commit。

# pnpm + prisma monorepo 配置
# prisma generate 需要 @prisma/client 在 root node_modules 可见
# 否则 pnpm 严格隔离会导致 "Could not resolve @prisma/client" 错误
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=@prisma/client

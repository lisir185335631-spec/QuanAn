# 依赖
node_modules
.pnpm-store

# 构建产物
dist
dist-admin
build
.next
*.tsbuildinfo

# 环境变量(★ 严格不传)
.env
.env.local
.env.production
.env.development.local
.env.test.local

# 测试 + 覆盖率
coverage
.nyc_output
test-results
playwright-report

# 日志
logs
*.log
npm-debug.log*
pnpm-debug.log*

# 编辑器 / OS
.DS_Store
.idea
.vscode/settings.json
*.swp

# Prisma
prisma/migrations/dev.db*
prisma/.env

# Vercel / Cloudflare
.vercel
.wrangler

# Coding 3.0(运行时产物 · 仅 prd-N.json 原版入库 · 其余 ignore)
ralph-output.log
ralph-lock.json
audit-gate.json
cost-log.jsonl
# scripts/ralph/prd.json · 运行时副本(switch-prd.sh 拷贝 · ralph 修改)· 不入库
scripts/ralph/prd.json
scripts/ralph/*.start.json
scripts/ralph/*.done.json
scripts/ralph/progress.txt
# scripts/ralph/agent-logs/ · ralph 跑时写的 prompt log · 历史一次入库 · 新生成不入
scripts/ralph/agent-logs/
# scripts/ralph/backups/ · ralph 自动 prd.json 备份 · 同上
scripts/ralph/backups/
# scripts/ralph/__pycache__/ · python 缓存 · 不入库
scripts/ralph/__pycache__/
# scripts/ralph/verify-artifacts/ · audit-artifacts.py 产物 · 历史一次入库 · 新生成不入
scripts/ralph/verify-artifacts/
.agents/tech-debt.json.bak
.agents/plans/

# Codex / IDE 缓存
.codex/
.cache/

# pgvector source build(临时)
/tmp/pgvector-build/

# 临时
*.tmp
.cache
scripts/ralph/prd.json.bak.*
# 多 PRD cost-log · 跨 PRD 不入库
scripts/ralph/cost-log-*.jsonl
# progress.txt 手动备份
scripts/ralph/progress.txt.bak.*
# validator / agent-browser 自动截图(大文件 · 不入库 · 需要时 git add -f)
screenshots/
.gstack/
# Claude Code 内存文件
.claude/
# ralph 专用 prd 备份 · 非 prd.json 主副本
scripts/ralph/prd-*.json
# 调试截图/诊断产物
apps/api/*.png
apps/web/diagnosis-us002/
# apps/scripts 临时工具脚本
apps/scripts/ralph/
